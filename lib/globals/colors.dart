import 'package:flutter/material.dart';

const mainThemeColor = Color.fromARGB(255, 89, 122, 255);
const selectedItemColor = Colors.white;
const unselectedItemColor = Colors.grey;

// barTextColor
const barTextColor = Colors.white;

// Theme Background Color
const themeBackgroundColor = Colors.white;